# Supabase local/CLI config.
# Auth is verified inside dynamic-endpoint (admin role check).
# Disabling gateway JWT avoids 401 responses without CORS headers,
# which browsers report as "Failed to send a request to the Edge Function".

[functions.dynamic-endpoint]
verify_jwt = false
